angular.module('batchmodeCtrl', [])
    .controller('BatchModeCtrl', ['$scope', 'toaster', '$rootScope', '$http',
        function ($scope, toaster, $rootScope, $http) {
 
          
        }]);